namespace Engine {
	void run() {
		// create UI thread
		// init UI

		// init sockets

		// main loop
		// send broadcast
		// recieve broadcast
		// poll input events
		// send control messages
	}
}